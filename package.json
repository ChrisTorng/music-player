{
  "name": "music-player",
  "version": "1.0.0",
  "description": "Universal classical music player with multi-track audio/video synchronization",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "serve": "python3 -m http.server 8000",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "typescript": "^5.0.0"
  }
}
